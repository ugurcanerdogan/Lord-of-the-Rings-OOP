public class Orc extends Enemy {
    public Orc() {
    }

    public Orc(int healthPoint, int attackPoint, String name, boolean isDead) {
        super(healthPoint, attackPoint, name, isDead);
    }
}